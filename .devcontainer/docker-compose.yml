version: '3.8'
services:
  app:
    build: 
      context: ..
      dockerfile: .devcontainer/Dockerfile
    volumes:
      - ..:/workspace
      - synthea-data:/workspace/synthea/output
    environment:
      - PYTHONUNBUFFERED=1
      - JAVA_HOME=/usr/lib/jvm/java-11-openjdk-amd64
    ports:
      - "5000:5000"  # For Flask API
      - "3000:3000"  # For React frontend
    working_dir: /workspace
    command: sleep infinity  # Keep container running

volumes:
  synthea-data: